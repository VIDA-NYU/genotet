language: node_js
node_js:
  - "4.1"

before_script:
  - npm install -g gulp
  - npm install -g jasmine-node
  - bower install
script:
  - gulp
  - ./setup.sh
  - node server/server.js --config=test/config
  - gulp test
  - jasmine-node test/jasmine-node/

sudo: false
